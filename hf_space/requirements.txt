flask>=3
numpy>=1.24,<2.3
compress-fasttext>=0.1.5
scikit-learn>=1.3          # compress-fasttext imports sklearn at load (PQ encoder)
gensim>=4.3,<5
transformers>=4.44,<5
huggingface_hub>=0.24
# torch (CPU) is installed separately in the Dockerfile from the CPU wheel index.

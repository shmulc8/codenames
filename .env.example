# Copy to .env and fill in. .env is git-ignored.

# OpenAI key — used by research/ eval + data-curation scripts (not by the served app).
OPENAI_API_KEY=

# Hugging Face token with write access to the Space + feedback dataset.
# Used by app.py (feedback upload) and scripts/deploy.py (Space upload).
HF_TOKEN=

# Agent: stepA-1 — Scaffold, API client, app shell

You work in worktree `wt-a1`, branch `stepA-1`. Time budget: **45 minutes**. Read `agents/00-orchestration.md` and `agents/01-CONTRACTS.md` fully before writing code. You own exactly the files listed for stepA-1 in CONTRACTS §4 — touch nothing else. stepA-2 is working in parallel on the store, theme, mocks, and Playwright; do not create any of their files except `src/types/api.ts` (verbatim copy, CONTRACTS §2).

## Tasks (in order)

1. **Scaffold**: Vite + React 18 + TS strict. The repo dir is non-empty (`agents/`, `.git`) and the scaffolder may refuse it — scaffold into `tmp-scaffold/` and move the contents up (or write the files by hand; it's ~6 files). `index.html`: `<html lang="he" dir="rtl">`, title `קודנהיימס קופיילוט`, a system Hebrew font stack. `.gitignore` for node/vite/playwright artifacts. Also create `.gitattributes` with the line `agents/SYNC-REQUESTS.md merge=union` — four parallel branches append to that file, and union-merge makes those appends conflict-free.
2. **package.json**: install the FULL dependency list from CONTRACTS §5 (runtime + dev, including `@playwright/test`, `msw`, `tesseract.js`, `zustand`) and add all scripts from §5. This is the only package.json commit in the project — get it complete now so nobody else needs to touch it.
3. **vite.config.ts**: react plugin + the `/api` dev proxy from CONTRACTS §5.
4. **`src/types/api.ts`**: paste verbatim from CONTRACTS §2.
5. **API client — `src/api/client.ts`** (this file is the ONLY place the team-agnostic app touches the engine's `my/opp` wire vocabulary — CONTRACTS §1): one typed async function per endpoint: `getHealth()`, `getDeal()`, `postSpymaster(board, target, focus?, risk?)`, `postCheck(board, target, clue)`, `postSpace(board, target, clue?)`, `postFeedback(payload)`. Each request-building function takes a `target: TeamColor` and **wire-maps roles at the boundary**: a small `toWire(roles, target)` maps `target→'my'`, the other team color→`'opp'`, `neutral/assassin` unchanged; a matching `fromWire(roles, target)` maps role-bearing responses (`/api/space.roles`, spymaster read/leak if the engine returns wire) back to absolute colors. `getDeal()` maps its response `my→red, opp→blue` on ingest. `postFeedback` reads `payload.target` to wire-map `board.roles` + `revealed[].chosenBy` before sending. Shared `request()` helper: JSON in/out, prefix `import.meta.env.VITE_API_BASE ?? ''`, 30s timeout via AbortController, non-2xx or `{error}` body ⇒ throw `ApiError { message, status }` with the server's Hebrew message when present. **Never send `engine`/`model` fields.** Board payloads passed in are already live-words-only (callers use the `liveBoard` selector) — the client does not filter, only wire-maps.
   - **FIRST, before writing the mapping: hit the live `/api/deal` once and confirm the wire role tokens are `my|opp|neutral|assassin` (CONTRACTS §1).** If they differ (e.g. `team/rival`), change ONLY the `toWire`/`fromWire` tables — nothing else in the app uses these tokens.
6. **App shell**: `src/main.tsx` — if `import.meta.env.VITE_USE_MOCKS === '1'`, dynamically `import('./mocks/browser')` and `await worker.start()` before rendering (wrap in try/catch so a missing mocks dir during Phase A doesn't crash dev). `src/App.tsx` → renders `src/screens/MainScreen.tsx`. MainScreen composes per CONTRACTS §6: setup screen vs game screen, `tab-clue`/`tab-check` tabs bound to `store.activeTab`/`setActiveTab` (`data-testid` per §7), prop-less SemanticMap, always-rendered collapsible SessionLog. Since the store doesn't exist in your worktree during Phase A, guard the store import (`src/state/store` may be missing until merge): write MainScreen against the store API from CONTRACTS §3 — it only needs to compile after the 0:45 merge; until then keep `npm run typecheck` green by stubbing nothing else and accepting that this one import resolves at merge. If that bothers the build, use local `useState` for tabs with a `// TODO(merge): bind to store.activeTab` and swap it during the 0:45 merge window (you do the swap — it's your file). Keep layout simple: CSS grid, board center, panel right (visually right in RTL = start side). Match the shell composition in `agents/design/screens/desktop-3a-main.png` (board + right control column with tabs, clue panel, map, log).
7. **Feature stubs**: create every `src/features/<name>/index.tsx` from CONTRACTS §6, each exporting the frozen component name/props and rendering `<div data-testid="stub-<name>">בקרוב</div>`. Stubs may import from `../../types/api` (for prop types) but must NOT import the store.
8. **Verify**: `npm run typecheck` clean, `npm run dev` renders the shell with stubs.

## Sync

- **T+20min checkpoint**: commit + push everything you have (at minimum steps 1–4 done) with message `checkpoint: scaffold-ready`. stepA-2 merges this to get package.json + types.
- Done: commit `stepA-1 complete`, push. You merge to main FIRST at 0:45.

## Definition of done

Typecheck clean (the tab fallback with `// TODO(merge)` is acceptable until the store merges — you swap it during the 0:45 merge window); dev server shows RTL Hebrew shell with both tabs and all stubs; API client compiles against verbatim types; all deps from CONTRACTS §5 in package.json.

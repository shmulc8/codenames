# Agent: stepA-2 — Store, theme, mocks, Playwright

You work in worktree `wt-a2`, branch `stepA-2`. Time budget: **45 minutes**. Read `agents/00-orchestration.md` and `agents/01-CONTRACTS.md` fully first. You own exactly the stepA-2 files in CONTRACTS §4. stepA-1 is scaffolding in parallel — you do NOT create package.json, vite config, or app shell.

## Phase 1 (0:00–0:20, no npm needed — pure file authoring)

1. **`src/types/api.ts`**: paste verbatim from CONTRACTS §2 (byte-identical to stepA-1's copy — this is deliberate; git merges identical adds cleanly).
2. **Store — `src/state/store.ts`** (Zustand): implement the full `AppState` from CONTRACTS §3 (team-agnostic — `target: TeamColor`, NO `myColor`), every action and all selectors (`liveBoard`, `fullBoard`, `selectedColor`). Details: `toggleSelected` only accepts `red|blue` tiles and enforces one-color clusters — a word whose color ≠ the current selection's color is rejected via a Toast ("אפשר לבחור רק קלפים בצבע אחד"); when selection is empty, selecting a word sets `target` to that word's color. `setTarget(c)` clears `selected` (a cluster can't span colors). `toggleLifecycle` flips inPlay↔chosen, defaults `chosenBy` to the tile's own role when not given, sets `clue.stale = true`, and if `clue.used` exists appends `{word, chosenBy}` to `clue.used.revealedAfter` (only on inPlay→chosen); `useCurrentClue` builds a `UsedClue` from `clue.current.options[optionIndex]` + `s.target` + `fullBoard` snapshot, sets `clue.used`, pushes to `log`; `setBoard` resets tiles (all `inPlay`), clears selected/clue/log, sets screen `'game'`; `resetGame` back to `'setup'`.
3. **Theme**: copy **both** `agents/design/nocturne-tokens.css` (base design system — dark navy, blurple accent, ready `.btn/.seg/.card/.tag/.dialog/.input` classes; use them, don't reinvent) **and** `agents/design/tokens.css` (app `--cn-*` tokens: role colors, cream card, semantic map, chrome) → `src/theme/` and import both globally. Load **Noto Sans Hebrew** first in the body font stack (Inter has no Hebrew glyphs). Read `agents/design/DESIGN.md`, then add `src/theme/game.css` with team-agnostic role classes `role-red`/`role-blue`/`role-neutral`/`role-assassin` that each pull straight from the matching `--cn-<role>-*` tokens (ring, dot, grad, tint, shape) — **no `myColor`, no `data-mycolor` remapping; the color IS the role**. **Base components — `src/components/`**: `Button` (primary/ghost, disabled+loading states), `Panel` (titled card), `Toast` (simple global toast via tiny store or context; `data-testid="toast"`), `Tooltip` (title-attr based is fine), `RoleIcon` (role → red ◆ · blue ● · neutral − · assassin ☠, from `--cn-*-shape`, with color + `aria-label`). Keep them tiny — no framework.
4. **MSW — `src/mocks/`**: `browser.ts` (worker setup), `handlers.ts`, `fixtures/board.ts` exactly per CONTRACTS §8. `fixtures/board.ts` holds the board in **absolute colors** (9 red / 8 blue / 7 neutral / 1 assassin — the store's shape). Handlers run in **wire** space (the client wire-maps before sending), so posted roles arrive as `my|opp|neutral|assassin`; the `/api/deal` handler **returns** wire roles (`9 my / 8 opp / 7 neutral / 1 assassin`) and the client maps them back to absolute on ingest. Handlers must echo posted words for `/api/space` (deterministic pseudo-coords: hash word → angle, wire role → radius band: my 0.3, neutral 0.55, opp 0.7, assassin 0.35 so the assassin reads as "dangerously close"). `/api/coach/check`: `illegal: true` iff the posted clue is one of the posted board words. `/api/feedback`: return `{ok:true}`, but 500 once if `window.__failFeedbackOnce === true` (then clear the flag). **Every POST handler saves its last request body to the window hooks from CONTRACTS §8** (`__lastSpymasterReq`, `__lastCheckReq`, `__lastSpaceReq`, `__lastFeedback`) — all testers depend on these (note: these bodies are in wire space, since that's what the client sends). Add ~400ms artificial delay to coach endpoints so loading states are testable.

## Phase 2 (after SYNC A at 0:20)

5. **Merge stepA-1's checkpoint** (`git merge stepA-1` or `origin/stepA-1`) → you now have package.json + scaffold. `npm install`, `npx playwright install chromium`, `npx msw init public/`.
6. **Playwright — `playwright.config.ts`**: single chromium project, port-aware per CONTRACTS §5 (`PW_PORT`, default 5173 — yours is 5173): derive `baseURL` and `webServer` (`npm run dev:mock -- --port ${port} --strictPort`, `reuseExistingServer: false`) from it, trace on-first-retry. Commit `public/mockServiceWorker.js` (output of `npx msw init public/`) — everyone's tests need it. **`tests/support/helpers.ts`**: `setupDemoBoard(page)` — navigate `/`, click `btn-skip-demo` (stepB-1 will implement it; until then this helper is written but only used by later specs), plus small utilities (`tile(page, i)`, `expectToast`).
7. **Smoke test — `tests/e2e/smoke.spec.ts`**: app loads, `dir=rtl` on html, both tabs visible and switchable, all feature stubs render. Must pass now: `npm run test:e2e`.
8. **Verify store**: add `tests/e2e/store.spec.ts`? No — keep store verification inside smoke via a temporary window hook: expose the store in dev/mock mode as `window.__store` (do this in `store.ts`, guarded by `import.meta.env.DEV`); smoke test drives `setBoard` + `toggleLifecycle` through it and asserts selectors' output. This hook is also what testers use later — keep it.

## Sync

- If stepA-1's checkpoint is late, continue Phase 1 polishing; do not scaffold your own package.json.
- Done: commit `stepA-2 complete`, push. You merge to main SECOND at 0:45 (after stepA-1). Expected conflicts: none (disjoint files; types file identical). If one appears on a file you don't own, take theirs.

## Definition of done

Typecheck clean after merge; `npm run test:e2e` green (smoke passes with MSW); store fully implements CONTRACTS §3 incl. `window.__store` dev hook; mock handlers cover all 6 endpoints deterministically.

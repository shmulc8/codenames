flask>=3
numpy>=1.24,<2.3
# The geometry engine loads the compressed fp16 fastText model (loss-free, ~250 MB) — the
# full 7 GB cc.he.300.bin is not needed. compress-fasttext pulls in gensim; sklearn is used
# by its PQ encoder at load time.
compress-fasttext>=0.1.5
scikit-learn>=1.3
gensim>=4.3,<5
# transformers pinned <5 — NeoDictaBERT emits NaN embeddings on 5.x
transformers>=4.44,<5
torch>=2.2
sentence-transformers>=3.0
huggingface_hub>=0.24
# Apple-Silicon only — local DictaLM 3.0 spymaster/guesser (OPTIONAL; the fastText
# geometry engine is the default and needs none of this).
mlx-lm>=0.16 ; platform_system == "Darwin"
# notebook for the alignment probe (optional)
jupyterlab
